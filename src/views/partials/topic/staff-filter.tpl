		<div class="topic-main-buttons float-end">
			<button id="staff-filter-btn" class="btn btn-sm btn-outline-secondary {{{ if staffOnly }}}active{{{ end }}}">
				{{{ if staffOnly }}}
				Show All Posts
				{{{ else }}}
				Show Staff Posts Only
				{{{ end }}}
			</button>
		</div>